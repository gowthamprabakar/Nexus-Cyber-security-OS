case_id: 14_skill_first_of_class_pending
description: >
  Skill trigger fires, eval-gate passes, but the (agent_id, category)
  class is new (not in registry) → candidate notification written to
  workspace root, skill_id added to pending_operator_review. Shadow
  + sidecar remain on disk for CLI approval.
fixture:
  agents:
    - agent_id: investigation
      default_passed: true
      case_count: 3
      fail_when_overlay: false
      audit_entries:
        - action: tool.call
          payload:
            tool_name: memory_neighbors_walk
          entry_hash: h0
        - action: tool.call
          payload:
            tool_name: ocsf_lookup
          entry_hash: h1
        - action: tool.call
          payload:
            tool_name: iam_query
          entry_hash: h2
        - action: tool.call
          payload:
            tool_name: s3_get
          entry_hash: h3
        - action: tool.call
          payload:
            tool_name: lambda_invoke
          entry_hash: h4
  skill_lifecycle_enabled: true
  llm_responses:
    - |
      ---
      name: test-skill
      description: Detect IAM privilege escalation via cross-account role chain.
      version: 0.1.0
      platforms:
        - nexus
      target_agent: SHOULD_BE_OVERRIDDEN
      category: iam-privesc
      created_by: llm-stub
      ---
      # Test Skill

      When you see cross-account AssumeRole chains, follow the chain head-first.
  semantic_store: false
expected:
  total_agents_evaluated: 1
  successful_runs: 1
  regressions_count: 0
  ab_present: false
  skill_pending_review_count: 1
  skill_deployment_count: 0
  notification_file_exists: true
  candidate_meta_exists: true
  markdown_contains:
    - 'Meta-Harness Report'
  scorecard_upserts: 0
