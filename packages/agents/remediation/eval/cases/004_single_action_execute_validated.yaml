case_id: 004_single_action_execute_validated
description: |
  One finding; execute mode; dry-run + execute succeed; post-validation detector sees no
  surviving findings → outcome EXECUTED_VALIDATED. No rollback.
fixture:
  mode: execute
  authorization:
    mode_recommend_authorized: true
    mode_dry_run_authorized: true
    mode_execute_authorized: true
    authorized_actions:
      - remediation_k8s_patch_runAsNonRoot
    max_actions_per_run: 5
    rollback_window_sec: 300
  # Promotion-state assumption: execute mode requires Stage 3+. The
  # action class has graduated through Stage 1 → 2 → 3 with two prior
  # sign-offs. Stage 4 is globally closed in v0.1.1; execute mode caps
  # at Stage 3 for this case.
  promotion:
    schema_version: '0.1'
    cluster_id: eval-fixture
    created_at: '2026-05-17T00:00:00Z'
    last_modified_at: '2026-05-17T00:00:00Z'
    action_classes:
      remediation_k8s_patch_runAsNonRoot:
        action_type: remediation_k8s_patch_runAsNonRoot
        stage: 3
        sign_offs:
          - event_kind: advance
            operator: eval-fixture
            timestamp: '2026-05-17T00:00:00Z'
            reason: eval fixture graduated to Stage 2 (dry-run threshold)
            from_stage: 1
            to_stage: 2
          - event_kind: advance
            operator: eval-fixture
            timestamp: '2026-05-17T00:00:01Z'
            reason: eval fixture graduated to Stage 3 (execute testing)
            from_stage: 2
            to_stage: 3
  findings:
    - rule_id: run-as-root
      rule_title: Container running as root
      severity: high
      workload_kind: Deployment
      workload_name: api
      namespace: production
      container_name: api
      manifest_path: cluster:///production/Deployment/api
      detected_at: '2026-05-16T12:00:00Z'
  dry_run_result:
    exit_code: 0
  execute_result:
    exit_code: 0
  post_validate_findings: [] # clean → validated
expected:
  finding_count: 1
  by_outcome:
    executed_validated: 1
