case_id: 003_single_action_dry_run
description: One finding; dry-run mode + successful dry-run; outcome DRY_RUN_ONLY.
fixture:
  mode: dry_run
  authorization:
    mode_recommend_authorized: true
    mode_dry_run_authorized: true
    mode_execute_authorized: false
    authorized_actions:
      - remediation_k8s_patch_runAsNonRoot
    max_actions_per_run: 5
    rollback_window_sec: 300
  # Promotion-state assumption: dry_run mode requires Stage 2+. The
  # action class is pinned to Stage 2 by an "operator confirmed at least
  # one recommend-stage artifact" sign-off (the Stage 1 → 2 graduation).
  promotion:
    schema_version: '0.1'
    cluster_id: eval-fixture
    created_at: '2026-05-17T00:00:00Z'
    last_modified_at: '2026-05-17T00:00:00Z'
    action_classes:
      remediation_k8s_patch_runAsNonRoot:
        action_type: remediation_k8s_patch_runAsNonRoot
        stage: 2
        sign_offs:
          - event_kind: advance
            operator: eval-fixture
            timestamp: '2026-05-17T00:00:00Z'
            reason: eval fixture assumes Stage 2 for dry_run testing
            from_stage: 1
            to_stage: 2
  findings:
    - rule_id: run-as-root
      rule_title: Container running as root
      severity: high
      workload_kind: Deployment
      workload_name: api
      namespace: payments
      container_name: api
      manifest_path: cluster:///payments/Deployment/api
      detected_at: '2026-05-16T12:00:00Z'
  dry_run_result:
    exit_code: 0
    stdout: deployment.apps/api patched (dry run)
expected:
  finding_count: 1
  by_outcome:
    dry_run_only: 1
