# v0.5 backlog — accumulating during v0.4

> Items deferred to v0.5 that were **surfaced during v0.4 execution**. Each has an explicit
> **trigger** + **source** (same discipline as `2026-06-08-parked-architectural-work.md`: written
> down, not "we'll remember"). Not a blocker for any v0.4 work. Opened when v0.5 planning begins.

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Trigger to open | Source                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | --------------------------------------------------------------------------------------- |
| 1   | **NexusEnvelope universal-invariant ADR** — decide whether every agent's findings-file OCSF must carry the `NexusEnvelope` wrapper. Today 6 agents emit **bare** OCSF (appsec, curiosity, synthesis, investigation, remediation, audit); the rest wrap via `wrap_ocsf`. **appsec is the priority anomaly** — it's a detection agent in the same class as the wrapped detection agents, so its bare emission is most likely a real wiring gap (vs. audit/remediation/investigation which differ by OCSF class/lifecycle, and curiosity/synthesis which are LLM agents with distinct emission). | v0.5 planning   | Fleet Test L1 finding #1 (#770); [L1 close](../fleet-test-level-1-closed-2026-06-20.md) |
| 2   | **ADR-018 migration for raw entity_types** — `cve`, `ttp` (threat-intel), `hypothesis` (curiosity), `synthesis_report` (synthesis) are persisted as raw `entity_type` strings with no `NodeCategory` member. Add the enum members (or remap to existing categories) so these writers join the ADR-018 spine; update the agents' kg_writers + `fleet_testkit` harness assertions to use the enum.                                                                                                                                                                                              | v0.5 planning   | Fleet Test L1 finding #2 (#770); [L1 close](../fleet-test-level-1-closed-2026-06-20.md) |

## Notes

- Both items are documented in-test where they bite: the L1 wiring harnesses use bare-or-wrapped
  OCSF validation + accept raw entity_type strings, so the fleet stays green while the debt stays
  visible (no fake-greens).
- Other standing v0.5 deferrals tracked in their own close records (not duplicated here): D.10
  SSO_INTO, D.15 live-connector activation, DSPy production-flag flip (Gate-3 gated), findings-as-
  decorations graph migration, deep cascade depth ≥ 5.
