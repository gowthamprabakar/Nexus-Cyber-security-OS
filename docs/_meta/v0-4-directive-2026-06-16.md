# v0.4 Directive — 2026-06-16 (team draft v2, recon-verified, operator-decisions folded)

**Status:** TEAM DRAFT v2 for operator final review — recon-verified against main `6360079`; operator Q-v0.4 answers + #710-review decisions folded in.
**Source:** operator scaffolding + Q-v0.4 answers + #710 scope-correction (2026-06-16) + E-2 readiness audit (#709).
**Predecessor:** v0.3 / Phase D (closed 2026-06-15; v0.3 OPERATING).
**Discipline:** audit-first → recon → execute → verify-against-main → SURFACE → close.
**HOLD:** no v0.4 PRs until the operator approves this directive.

**Q-v0.4 (locked):** Q1 DEPTH-FIRST · Q2 HARD 85% · Q3 DSPy DELEGATED · Q4 continuous-autonomy DELEGATED (per-tenant gated) · Q5 BOTH D.10 SSPM + D.11 AI-SPM.
**#710-review decisions (locked):** graph foundation = **Option A (extend Postgres SemanticStore; Neo4j stays dormant)** · inventory discovery = **Option X (fold into Stages 1+2; Stage 3 wiring-only)** · Snowflake → **v0.5 defer** · D.11 = **(a) deployment discovery + (b) prompt injection** · D.5 collision → **data-security keeps D.5, multi-cloud-posture renames**.

---

## §0 — Premise corrections (read first)

- **P-1.** v0.3 OPERATING on main `6360079` (E-1 #708 / E-2 #709). Full repo 7339 pass / 0 fail; 0 substrate violations in the v0.3 lifecycle.
- **P-2.** v0.4 is structural-shape work (net-new agents + Hermes loop closure + the fleet-graph extension + inventory discovery), not cascade-shape. v0.3's velocity does NOT transfer; calendar is realistic (§12).
- **P-3.** Sequencing is DEPTH-FIRST. Mature agents (incl. their inventory discovery) feed graph input quality.
- **P-4.** Committed to the **85% PRD target**. If a stage slips, OPERATING delays — the commitment holds.
- **P-5.** Coverage figures are **`[estimate]`, never instrumented** (E-1/E-2). v0.4 adds instrumentation if it commits to a hard 85% _number_ (§13 R-1). This directive does not assert verified coverage.
- **P-6.** **Correlation commitment = 3-hop** (`MAX_TRAVERSAL_DEPTH = 3` in the Postgres SemanticStore). Deep cloud-to-code cascade (depth ≥ 5) is **DEFERRED to v0.5+**; the ADR-009 conditional Neo4j swap remains the escape hatch (scope discipline, Layer 34).

---

## §0.5 — Divergence + decision ledger (recon vs scaffolding; SURFACE)

Corrections folded into the stages below. RESOLVED = operator-locked in the #710 review.

| #    | Item                                 | Reality on `6360079` / decision                                                                                                                                                                                                                                                                                                                                                                                                                             | Status                     |
| ---- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| D-1  | Graph foundation                     | **RESOLVED — Option A: extend the Postgres `SemanticStore`.** It is already the live fleet graph (entities/relationships/`neighbors` BFS depth≤3/`list_entities_by_type`); 6 agents already write via `kg_writer.py`. Neo4j stays **dormant** (cloud-posture `neo4j_kg.py`, ADR-009 escape hatch for a v0.5+ depth≥4 swap). NOT a Neo4j stand-up.                                                                                                           | RESOLVED                   |
| D-2  | ADR numbering                        | ADR-016 (tool-proxy) + ADR-017 (v0-2-quality-gate) exist; ADR-013 is a never-created gap. v0.4 ADRs **start at 018**.                                                                                                                                                                                                                                                                                                                                       | folded                     |
| D-3  | Agent count                          | **18 packages** = 11 detection + 7 orchestration/LLM/safety (not "14").                                                                                                                                                                                                                                                                                                                                                                                     | folded                     |
| D-5  | D.4 Zeek-conn `FlowRecord`           | **Already shipped** (`normalize_zeek_conn` + `ZeekConn` + `FlowRecord`). Re-scoped: kg_writer wiring + network-topology discovery.                                                                                                                                                                                                                                                                                                                          | folded                     |
| D-6  | D.2 inline-policy fetch              | **Already exists v0.2** (enumeration + document); effective-perms simulator built + gated (A-4 #688). Re-scoped to per-role evaluation depth.                                                                                                                                                                                                                                                                                                               | folded                     |
| D-7  | D.3 "93 Falco rules"                 | Actually **4 default + hot-reload**; FIM absent; passive baseline exists, active anomaly is net-new.                                                                                                                                                                                                                                                                                                                                                        | folded                     |
| D-8  | D.5 sources / Snowflake              | Current S3/Azure/GCS. **RESOLVED — v0.4 = RDS + DynamoDB + BigQuery; Snowflake → v0.5** (honors agent README).                                                                                                                                                                                                                                                                                                                                              | RESOLVED                   |
| D-9  | Hermes Phase 2                       | **Partially built** (`adjudicate_pass_rates` + `skill_eval_gate`); net-new = fleet-wide/cross-skill adjudication. **Phase 4 T2 trace persistence is the real blocker**; Phases 3+5 greenfield.                                                                                                                                                                                                                                                              | folded                     |
| D-11 | **Inventory coverage gap**           | The SemanticStore covers only **~30% of typical org inventory** today (cloud-posture writes AWS assets/findings; compliance frameworks/controls; threat-intel IoC/CVE/TTP; synthesis/curiosity/meta-harness their own). **Missing sweeps:** Azure/GCP assets, multi-cloud network topology, K8s inventory, container runtime, network flows, databases, identity hierarchy, SCM repos, SaaS, AI/ML. **Inventory discovery ≠ detection** (Layer 33).         | RESOLVED (scoped via D-12) |
| D-12 | Stage 3 scope                        | **RESOLVED — Option X: fold inventory discovery into each agent's Stage 1/2 depth work.** Stage 3 stays **wiring-only (~4-5w)**. Stages 1+2 grow (~8-12w each).                                                                                                                                                                                                                                                                                             | RESOLVED                   |
| D-13 | D-numbering                          | **RESOLVED — data-security keeps D.5; multi-cloud-posture renames** (team picks a clean number; renames consistently). **⚠️ verify-against-main correction:** main already has **D.7 = Investigation (#8), D.8 = Threat-Intel (#12)**. The pending catalogue (per operator) labels "D.7 = Threat Intel" — that contradicts main → **correct the catalogue to match the code** (no code rename; lowest disruption), unless operator directs a code renumber. | RESOLVED + open item R-1   |
| D-14 | AppSec build status                  | The pending catalogue (per operator) marks "D.9 AppSec — Unbuilt." Reality: **AppSec is D.14, built v0.1 in v0.3 B-1** (#690-707 on main). Catalogue to be corrected.                                                                                                                                                                                                                                                                                       | folded                     |
| D-15 | kg-writing agents                    | **⚠️ verify-against-main correction:** the Part-2 land message claims "investigation has `kg_writer.py` — verified." **False on main** — investigation has none. Actual kg-writers (6): cloud-posture, compliance, threat-intel, synthesis, curiosity, meta-harness. audit/supervisor/investigation do not write inventory.                                                                                                                                 | folded                     |
| D-16 | **Inventory catalogue not received** | The operator reports the catalogue delivered ahead of schedule, but **the document was not attached/received by the team and is absent from the repo**. **Land BLOCKED on receipt.** §11 keeps it PENDING; not marked delivered.                                                                                                                                                                                                                            | OPEN — needs operator      |

---

## §1 — Scope + boundaries

**IN SCOPE:** Stage 1 agent depth + inventory discovery · Stage 2 Hermes 2-5 + D.10 SSPM + D.11 AI-SPM (+ their inventory discovery) · Stage 3 fleet-graph wiring · Stage 4 Wazuh 12-item · Stage 5 close. Hard 85%; 3-hop correlation.

**OUT OF SCOPE (→ v0.5):** fully-autonomous continuous loop (v0.4 = per-tenant gated); deep cascade correlation (depth ≥ 5) + Neo4j swap; ML runtime baselining; Snowflake; net-new agents beyond D.10/D.11; surface/UI; cross-tenant correlation.

**Discipline carried:** L1-15 audit→recon→execute→verify; L19 substrate seal; L22 state-claim; L23 transcription; L24 credential; L25 PR-padding; L26 rebase-hazard; L27 rebase-as-checkpoint; L28 defer-to-operator-framing; **L29 delegate-technical-execution**.

---

## §2 — Executive summary

```
Commitment:  Hard 85% [estimate] coverage at OPERATING; 3-hop correlation
Sequencing:  Depth-first (Stage 1 → 2 → 3 → 4 → 5); Stage 3 design parallel
             with Stage 1 once the catalogue lands
Graph:       extend Postgres SemanticStore (NOT Neo4j); Neo4j dormant (ADR-009)

Stage 1  Agent depth + inventory discovery   ~8-12 weeks  (deeper per Option X)
Stage 2  Hermes 2-5 + D.10 + D.11 (+ disc.)  ~8-12 weeks  (parallel w/ late S1)
Stage 3  Fleet-graph wiring (wiring-only)    ~4-5 weeks
Stage 4  Wazuh 12-item enrichment            ~1-2 weeks   (parallel w/ late S3)
Stage 5  v0.4 close + v0.5 readiness         ~1-2 weeks

v0.4 calendar: ~22-30 weeks (compresses toward ~20-28 once the catalogue lands
               and Stage 3 design runs parallel — D-16/Layer 35).
```

---

## §3 — Stage 1: agent depth + inventory discovery (~8-12 weeks)

**Goal:** the 11 detection agents reach full maturity AND complete their domain **inventory discovery** (Option X). Each sub-milestone's inventory scope ties to the inventory catalogue **once received** (D-16). _Per-agent depth = self-merge cascade; Track B = per-PR review._

- **1.1 — A-5 D.3 Runtime (~2-3w):** FIM (net-new) · Falco rule-pack expansion (from 4 default + hot-reload) · Tracee eBPF depth · osquery multi-query · active anomaly on the existing passive baseline (heuristic) · **+ runtime inventory discovery** (processes/files/containers) **+ `kg_writer.py`** (net-new — D.3 has none).
- **1.2 — A-6 D.5 Data-security (~2-3w):** **+ DB sources RDS + DynamoDB + BigQuery** (Snowflake → v0.5, D-8) · **+ database inventory discovery + `kg_writer.py`** (net-new).
- **1.3 — A-3 D.6 k8s-posture (~1-2w):** CIS v1.8 → **v2.0** (authoritative data; L23 transcription; Trigger #23) · **+ K8s inventory discovery** (namespaces/workloads/pods/services/RoleBindings/NetworkPolicies/IRSA) **+ `kg_writer.py`** (net-new).
- **1.4 — D.4 network-threat (re-scoped, D-5):** `FlowRecord` exists → **`kg_writer.py` wiring + network-topology discovery** (flows/reachability), not a new schema.
- **1.5 — D.2 identity (re-scoped, D-6):** inline-policy fetch exists → **drive gated per-role effective-perms evaluation into depth** + identity-hierarchy inventory (AAD groups, GCP SAs) **+ `kg_writer.py`** (net-new).
- **1.6 — Track B AppSec D.14 B-2/B-3/B-4 (~3-4w, parallel, per-PR review):** SAST language expansion · multi-tenant scale + perf · rate-limit/retry hardening · **+ SCM repo inventory discovery + `kg_writer.py`** (net-new).

**Close:** 11 detection agents mature + writing domain inventory to the SemanticStore; substrate seal preserved; per-sub-milestone verification doc.

---

## §4 — Stage 2: Hermes 2-5 + D.10 SSPM + D.11 AI-SPM (~8-12 weeks, parallel w/ late Stage 1)

### Stage 2a — Hermes Phases 2-5 [Q-v0.4-3 DELEGATED; team brainstorm → operator reviews concerns]

- **2a.1 Phase 2 — adjudication:** extend the existing `adjudicate_pass_rates` + `skill_eval_gate` (D-9) to **fleet-wide / cross-skill** adjudication + golden-case framework + decision persistence.
- **2a.2 Phase 3 — cross-agent skill sharing:** greenfield; shared registry + per-agent compatibility.
- **2a.3 Phase 4 — T2 trace persistence + DSPy cadence activation:** **the real blocker** — SemanticStore trace schema (ADR-018), per-tenant cost ceiling, rollback; unblocks the default-OFF DSPy flag.
- **2a.4 Phase 5 — deprecation/lifecycle:** greenfield; degradation detection + deprecation/promotion.
  _Per-PR review._

### Stage 2b — net-new agents (parallel; per-PR review)

- **2b.1 D.10 SSPM (~3-4w):** Salesforce / Slack / GitHub Enterprise / Google Workspace / M365 / HubSpot posture **+ SaaS inventory discovery + `kg_writer.py`**. OPEN: connector sequencing + OAuth scopes (rec: Salesforce/GitHub/Slack first).
- **2b.2 D.11 AI-SPM (~3-4w):** **scope LOCKED = (a) deployment discovery + (b) prompt-injection detection** (Garak + PyRIT + LLM Guard). `(c)` model-file scanning + `(d)` training-data sensitivity → **v0.5**. **+ AI/ML inventory discovery + `kg_writer.py`**.

**Close:** D.10 + D.11 operational + writing inventory; Hermes loop closed (Phases 1-5); T2 operational (default-OFF gated); substrate seal preserved.

---

## §5 — Stage 3: fleet-graph extension (wiring-only, ~4-5 weeks)

> **Terminology (Layer 32):** this is a **fleet-graph extension of the Postgres `SemanticStore`**, NOT an "inventory graph" stand-up and NOT Neo4j. Discovery sweeps land in Stages 1+2 (Option X); Stage 3 is wiring + queries + migration only.

**Pre-flight GATE:** operator delivers the **inventory catalogue** (currently PENDING — D-16). Stage 3 _design_ may begin in parallel with Stage 1 **once the catalogue lands** (Layer 35); Stage 3 _close_ still requires Stages 1+2 mature (agents must discover before wiring). Trigger #46 (catalogue-gap PAUSE) active.

- **A** — codify the catalogue as SemanticStore entity/relationship types.
- **B** — catalogue (operator artifact; D-16).
- **C** — wire the **remaining agents' `kg_writer.py`** (the 12 inventory-writing agents incl. the Stage 1/2 net-new writers); per-agent ownership → seal #19 stays clear. _Self-merge._
- **D** — `kg_query` read surfaces (**3-hop** blast-radius / attack-path; P-6) + findings-as-decorations migration (OCSF anchors to graph nodes). _Per-PR review (shared interface)._
- **E** — migration + verification.

Edge typology (from the catalogue, once received): VULNERABLE_TO / HAS_ACCESS_TO / IN_SUBNET / CONTAINS / RUNS_IMAGE / EXPLOIT_EXISTS_FOR / BUILT_FROM / TRAINED_ON / CAN_REACH / …

**Close:** catalogue codified; all inventory-writing agents on the graph; findings anchored; 3-hop queries live; substrate boundary documented. No new datastore (Postgres).

---

## §6 — Stage 4: Wazuh 12-item enrichment (~1-2 weeks)

**GATE:** operator delivers the **Wazuh 12-item spec** (PENDING; ~Week 18-22): WHAT 12 items · WHICH agents enrich · INTEGRATION pattern.

- **4a extraction** — clean-room (GPL-isolated; ADR-008 7-point provenance; facts only, no GPL code ships; Trigger #49).
- **4b integration** — agents reference enrichment facts (compliance attribution / ATT&CK tagging).
  **Close:** 12 items extracted clean-room; substrate clear; agents enriched; verification doc.

---

## §7 — Stage 5: v0.4 close + v0.5 readiness (~1-2 weeks)

- **5a** v0.4 completion record (mirror E-1 #708).
- **5b** v0.5 readiness audit (mirror E-2 #709): dimensions = net-new candidates / depth residuals / Hermes cross-agent + autonomous triggers / full continuous autonomy / **deep cascade correlation (depth≥5) + Neo4j swap readiness** / Snowflake + D.11 (c)/(d) + AI-SPM expansion. Operator Q-set.

---

## §8 — Pause triggers

**Carried:** #19 substrate seal · #20 new scope · #23 transcription/CIS-numbering (1.3 + Stage 4) · #29 shared-writer contention (Stage 3) · #35 trio-deploy-breach · #42 safety-critical substrate · #43 recon-premise discrepancy (fired §0.5) · #44 standards-numbering · #45 pre-checkpoint Layer-27.
**New:** **#46** catalogue-gap PAUSE · **#47** D.11 scope (now LOCKED a+b; trigger covers mid-impl divergence) · **#48** graph-substrate divergence (new datastore / shared kg interface vs per-agent ownership) · **#49** Wazuh GPL contamination · **#50** continuous-activation safety (per-tenant gating / cost ceilings / alarms in 2a.3).

---

## §9 — Substrate boundaries + anticipated ADRs (start at 018, D-2)

```
Stage 1: no substrate (per-agent depth + per-agent kg_writer)
Stage 2a: SemanticStore T2 trace schema → ADR-018
Stage 2b: net-new agents D.10/D.11 → per-agent ownership → ADR-019 / ADR-020
Stage 3: extend SemanticStore (Postgres) — NO new datastore. kg_query/shared
         interface note → ADR-021 if the read interface is fleet-shared.
Stage 4: Wazuh enrichment provenance → ADR-022 (extends ADR-008); facts only

NO Neo4j ADR in v0.4 (Option A locked; ADR-009 escape hatch unchanged).
All ADRs = per-PR review; team drafts in stage planning.
```

---

## §10 — Per-PR-review classification

**Per-PR review:** Stage 1.6 Track B · Stage 2a Hermes 2-5 · Stage 2b.1 D.10 · Stage 2b.2 D.11 · Stage 3 D (kg_query/shared interface) · all ADRs · all cycle-verification docs · the inventory-catalogue + Wazuh-spec lands.
**Self-merge cascade (auto-merge on green):** Stage 1.1-1.5 per-agent depth + per-agent kg_writer · Stage 3 C per-agent wiring · Stage 4 extraction + enrichment hooks · the multi-cloud-posture rename.
**Three merge paths (Layer 28):** self-merge cascade / manual click / **enable-auto-merge** (operator enables once → merges on green; the #706 path). **Layer 27 MANDATORY before any review signal:** rebase → CI green → THEN signal.

---

## §11 — Operator artifacts pending (gates)

1. **Inventory catalogue** — **PENDING / NOT RECEIVED (D-16).** Operator reports it ready ahead of schedule, but the document was not attached and is absent from the repo. **Land + Stage 3 design BLOCKED on receipt.** On receipt: land at `docs/_meta/v0-4-inventory-catalogue-2026-06-16.md` (per-PR review) + open `R-1/R-2/R-3` reconciliation items as a v1.1 amendment.
2. **Wazuh 12-item spec** — PENDING (~Week 18-22).
3. **R-1 (catalogue D-numbering):** main is authoritative (D.7=Investigation, D.8=Threat-Intel); confirm the catalogue is corrected to match, or direct a code renumber.

**Resolved (no longer pending):** graph foundation (Option A) · Stage 3 scope (Option X) · Snowflake (v0.5) · D.5 collision (multi-cloud-posture renames) · D.11 scope (a+b).

---

## §12 — Calendar projection (`[estimate]`)

```
Week 1-2     Stage 1 pre-flight + recon + brainstorms
Week 3-12    Stage 1 (depth + inventory discovery; deeper per Option X)
Week 8-18    Stage 2 (2a Hermes + 2b D.10/D.11 parallel)
[on catalogue receipt] Stage 3 DESIGN begins in parallel (Layer 35)
Week 16-22   Stage 3 wiring (after Stages 1+2 mature; ~4-5w execution)
Week 18-22   OPERATOR: Wazuh 12-item spec due
Week 22-25   Stage 4 (parallel w/ late Stage 3)
Week 24-26   Stage 5 close + v0.5 readiness

v0.4 OPERATING: ~Week 24-28 (60% confidence); ~22-30 range;
compresses toward ~20-28 once the catalogue lands early (parallel Stage 3 design).
```

---

## §13 — Risk register

- **R-1 coverage `[estimate]`** (HIGH/MED) — hard 85% _number_ needs instrumentation.
- **R-2 catalogue completeness + receipt** (HIGH/MED) — D-16 (not received) + Trigger #46; extensibility-first schema.
- **R-3 D.11 scope** — LOCKED a+b (de-risked); c/d → v0.5.
- **R-4 graph-foundation** — RESOLVED (Option A Postgres); R-4 retired.
- **R-5 Hermes T2 cost** (MED/MED) — per-tenant cap + alarms (Trigger #50); opt-in.
- **R-6 SaaS OAuth complexity** (MED/LOW) — sequence Salesforce/GitHub/Slack first.
- **R-7 Wazuh GPL** (HIGH/LOW) — ADR-008 provenance; facts-only (Trigger #49).
- **R-8 velocity dilution** (MED/HIGH) — structural ≠ cascade; calendar realism.
- **R-9 rebase hazards over a long calendar** (MED/HIGH) — L26 + L27 mandatory.
- **R-10 inventory discovery underestimate** (MED/MED) — D-11/Layer 33: sweep work (~6-10w embedded in Stages 1+2) is larger than wiring; calendar reflects the deeper Stages 1+2.

---

## §14 — Discipline matrix (additions)

L1-29 carried (§1). New in v0.4:

- **L30** catalogue-gap PAUSE (Trigger #46).
- **L31** team-recon-against-operator-draft is the institutional pattern (recon a from-memory scaffolding vs main before adopting it; this directive's §0.5).
- **L32** framing-confusion-naming: name substrates precisely — "fleet-graph extension (SemanticStore)", not "inventory graph"; reset misleading framing on sight.
- **L33** inventory-discovery ≠ detection: sweeping a domain's inventory is distinct, larger work than detecting in it; scope + estimate it separately.
- **L34** scope discipline > technical ambition: Postgres locked over a Neo4j cascade case because the depth-3 capability doesn't justify a second datastore + RLS loss this cycle.
- **L35** operator-delivered artifacts shift work left: an early gate artifact lets downstream design run in parallel rather than block (Stage 3 design ∥ Stage 1) — realized on actual receipt (D-16).

---

## §15 — Status + handoff

```
LOCKED:   Q-v0.4; depth-first; hard 85%; both D.10+D.11; Option A (Postgres);
          Option X (fold discovery into S1+2); Snowflake→v0.5; D.11=a+b;
          D.5 collision (multi-cloud-posture renames); 3-hop correlation;
          triggers (carried + #46-50); per-PR classification; L30-35.
CORRECTED (recon, §0.5): ADR→018; Stage 1.4/1.5 re-scoped; D.3 rule count;
          Hermes Phase-2 partial; agent counts/IDs; kg-writer roster (D-15);
          AppSec built (D-14); catalogue D.7 vs main (D-13/R-1); coverage=[estimate].
OPEN (operator): D-16 inventory catalogue NOT RECEIVED (land + Stage 3 design
          blocked on receipt) · R-1 catalogue D-numbering · Wazuh 12-item spec.
HOLD:     no v0.4 PRs until this directive is approved.

HANDOFF: operator delivers the catalogue + approves this directive → team writes
Stage 1 brainstorms → Stage 1 execution (Stage 3 design ∥ on catalogue receipt)
→ ... → Stage 5 → v0.4 OPERATING.
```

— team draft v2, 2026-06-16
