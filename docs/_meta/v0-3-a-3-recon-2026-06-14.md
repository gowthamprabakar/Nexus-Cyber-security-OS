# v0.3 / Phase D — A-3 recon: depth investment (CSPM breadth + "MCP" + k8s-posture) (2026-06-14)

> **Status:** Recon findings (doc-only, NO code). A-3 is the **depth/breadth** workstream
> (distinct from A-1 wiring). Implementation does NOT start until the operator picks the paths
> below. Net-new scanner build (Azure/GCP) was reclassified _into_ A-3 per Q-A-1 (2026-06-14).

## Area 1 — CSPM rule breadth (F.3 cloud-posture + D.5 multi-cloud-posture)

### Current state (the honest counts)

Native/stable rule breadth is **small**; "live ≠ rule-breadth" holds:

| Cloud | Native boto3/SDK checks                            | CIS controls mapped (stable) | Source                                                                                     |
| ----- | -------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------ |
| AWS   | ~3 native (MFA-less users, admin `*:*`, S3 enrich) | ~11 rule-IDs / 43 CIS rows   | `compliance/control_libraries/cis_aws_v3.yaml`; `agent-capability-vs-wiz-2026-06-07.md:35` |
| Azure | ~8 native (v0.2 net-new)                           | 8 rule-IDs / 14 CIS rows     | `cis_azure_v2.yaml`                                                                        |
| GCP   | ~5 native IAM-binding                              | 10 rule-IDs / 13 CIS rows    | `cis_gcp_v2.yaml`                                                                          |

F.3 AWS leans on **Prowler 5.x as a subprocess** (`cloud_posture/tools/prowler.py:24-76`) — the
wrapper delegates detection to the binary's built-in library and only post-filters by severity;
it does **not** author rules in-repo. Measured CSPM coverage held ~84% across v0.1→v0.2 (live
matured the liveness axis, not breadth).

### Depth-investment paths (operator fork A-3-1)

- **(P-A) Hand-author native rules** (boto3/Azure-SDK/GCP-Asset queries): zero substrate cost,
  slow accumulation (~10-20 AWS rules/cycle). The honest, license-clean default.
- **(P-B) Expose Prowler's native library + map IDs→CIS** (RECOMMENDED first move): Prowler
  already integrated (Apache-2.0); ships 700+ AWS checks. Work is _mapping_ Prowler IDs/severity
  to CIS numbering + compliance-YAML expansion, not new scanners. Low effort, immediate breadth.
- **(P-C) Steampipe for multi-cloud:** broad Azure/GCP coverage via SQL mods, BUT **SSPL/Elastic
  license (#23 review REQUIRED)** + a new `packages/shared/steampipe/` SQL-runner harness
  (substrate touch). Trial-in-sandbox only; defer if licensing unclear.
- **(P-D) Cloud Custodian:** Apache-2.0 (clean), 1000s of rules, but very high effort
  (policy-DSL→Python translator + read-only enforcement separation). v0.4-scope.

**License flags (#23):** Prowler Apache-2.0 ✅; Cloud Custodian Apache-2.0 ✅; **Steampipe SSPL —
STOP-and-review before adoption.**

## Area 2 — "MCP scanners": premise correction (NOT Model Context Protocol)

**Honest finding to surface:** a repo-wide search for `mcp`/`MCP` returns **3 hits, all in
strategy docs, zero code.** "MCP" in the v0.3 docs is shorthand for the **Multi-Cloud Posture
agent (D.5)**, _not_ the LLM Model-Context-Protocol. The Q-A-1 decision
(`v0-3-track-a-1-launch-2026-06-14.md:27-31`) reads: _"multi-cloud-posture's net-new Azure/GCP
findings scanners → reclassified to Track A-3 (CSPM rule breadth), where net-new scanner build
belongs."_

**Therefore there is NO MCP-server-scanning capability to build or wire** — Area 2 collapses
_into_ Area 1 (the D.5 Azure/GCP native-scanner build is the "MCP→A-3" item). If the operator
_also_ wants a genuine Model-Context-Protocol-server security scanner, that is **net-new scope
(#20)** and should be surfaced as its own brainstorm, not assumed here.

## Area 3 — k8s-posture runners (live built; RBAC heuristic)

### Current state

- **kube-bench live:** `k8s_posture/tools/kube_bench_live.py:1-54` — live subprocess exec via
  injectable `KubeBenchRunner`, shared parser with the offline path (byte-identical). Production-ready.
- **Polaris live:** `k8s_posture/tools/polaris_live.py:1-54` — parallel structure, production-ready.
- **kubelet runtime:** `k8s_posture/tools/kubelet_client.py:1-45` — read-only HTTP `/pods` +
  `/stats/summary`. Partial (no runtime-behavior detection — that's runtime-threat agent scope).
- **CIS catalog:** `k8s_posture/cis/benchmark.py:1-66` — **16 CIS K8s v1.8 controls** (representative
  subset, not the full benchmark).
- **RBAC:** `k8s_posture/rbac/over_privileged.py:1-84` — **heuristic only** (wildcard perms,
  broad secret-read, cluster-admin→SA binding). In-code: _"NOT a full effective-permissions
  simulator … that … is v0.3."_
- **Manifest rules:** ~10 native (run-as-root, privileged, hostNet/PID/IPC, privesc, missing-limits,
  imagePull, RO-rootfs, SA-token) per `agent-capability-vs-wiz-2026-06-07.md:35`.

### Depth-investment paths (operator fork A-3-2)

- **(Q-A) Expand manifest rules + bump CIS v1.8→v2.0** (RECOMMENDED first): low effort, pure
  Python, ~16→~40 controls; no substrate touch.
- **(Q-B) Full RBAC effective-perms simulator** (resolve ClusterRoles × Roles × Bindings):
  high-value, medium effort; **shares the D.2 effective-perms shape (see A-4 recon)** — candidate
  for a hoisted `rbac_simulator` IF both D.2 and k8s-posture consume it (substrate fork — operator
  decides; default agent-local until a 2nd consumer is real).
- **(Q-C) Admission-controller coverage** (Validating/Mutating webhooks): API-heavy, defer.

## Cross-cutting forks for operator decision

1. **A-3-1 CSPM path:** Prowler-ID→CIS mapping (P-B, low cost) vs Steampipe trial (P-C, SSPL #23
   review). Recommend P-B now, P-A ongoing, P-C only after license review.
2. **A-3-2 k8s depth:** ship heuristic RBAC to v0.3 + manifest/CIS expansion (Q-A) now; full RBAC
   simulator (Q-B) as a follow-up.
3. **Shared-simulator hoist:** if BOTH D.2 (A-4) and k8s-posture (Q-B) build effective-perms,
   hoist to `packages/shared`? **Defer until a 2nd real consumer exists** (the standing
   2-consumer hoist rule) — do NOT hoist speculatively (#19/#29).
4. **Genuine MCP scanner?** If wanted, it's net-new (#20) → separate brainstorm, not A-3.

## Substrate (#19/#29) summary

- Areas 1 (P-A/P-B) and 3 (Q-A) are **agent-local + additive** — substrate CLEAR.
- Steampipe (P-C) and a hoisted RBAC simulator (Q-B→shared) **would touch substrate** → operator-gated.

## References

- Counts — `cis_aws_v3.yaml` / `cis_azure_v2.yaml` / `cis_gcp_v2.yaml`; `agent-capability-vs-wiz-2026-06-07.md:35`.
- Prowler wrapper — `cloud_posture/tools/prowler.py:24-76`. CSPM ~84% — `f-3-cloud-posture-v0-2-verification-2026-06-08.md:93`.
- "MCP→A-3" — `v0-3-track-a-1-launch-2026-06-14.md:27-31`.
- k8s runners — `kube_bench_live.py` / `polaris_live.py` / `kubelet_client.py`; RBAC `over_privileged.py:1-84`; CIS `cis/benchmark.py:1-66`.
- A-3 recon agent (2026-06-14) — depth-investment sweep.
