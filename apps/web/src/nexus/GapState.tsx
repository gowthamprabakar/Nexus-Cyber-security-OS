// Placeholder — replaced by a later task
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function GapState({ needs: _needs, title: _title }: { needs: string; title: string }) {
  return null;
}
